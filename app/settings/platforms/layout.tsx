import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Platform Settings · GD' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
