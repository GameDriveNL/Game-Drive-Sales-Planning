import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Coverage Backfill' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
